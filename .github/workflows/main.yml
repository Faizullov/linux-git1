name: Run tests on any Push event
on:
  push:
   
jobs:
  check-code:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@master
     - uses: actions/setup-python@v1
       with:
         python-version: '3.8'
         architecture: 'x64'
     - name: Install requirements
       run: pip install -r project2/requirements.txt
     - name: Run tests
       run: |
        pip install pytest
        python -m pytest --import-mode=append project2/tests/
#      - name: Tests report
#        run: coverage report
